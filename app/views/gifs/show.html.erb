<p id="notice"><%= notice %></p>

<div>
  <%= image_tag @gif.image_url %>
</div>

<p>This gif was uploaded by <strong><%= @gif.user.name %></strong></p>
<p>Gif tags:</p>

<p>
  <%= linked_tag_list @gif %>
</p>

<% if user_signed_in? && current_user == @gif.user %>
  <%= link_to 'Edit', edit_gif_path(@gif) %> |
  <%= link_to 'Destroy', @gif, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
